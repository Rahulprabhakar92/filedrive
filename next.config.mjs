/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        remotePatterns:[
            {
                hostname: 'outstanding-peccary-207.convex.cloud',
            }
        ]
           
        
    }
};

export default nextConfig;
